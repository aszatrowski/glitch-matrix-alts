name: mgse
channels:
  - conda-forge
  - bioconda
  - defaults

dependencies:
  - python=3.10.13
  - pandas=2.1.4
  - pip
  - pip:
    - xftsim==0.2.0


# try building base environment with snakemake and conda, not mamba; solving happens via libmamba anyway and maybe it won't redirect through midway's ancient (????) conda install